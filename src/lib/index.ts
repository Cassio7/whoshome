// place files you want to import through the `$lib` alias in this folder.
export interface Persona {
	id: number;
	name: string;
	surname: string;
	username: string;
	device: string;
	active: boolean;
}